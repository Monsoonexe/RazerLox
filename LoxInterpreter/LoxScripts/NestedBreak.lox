var a = 5; // this is a comment
var b = 0;

while (a > 0)
{
	while (a > 1)
	{
		if (a == 2)
			break;
		a = a - 1;
	}
	b = b + 1;

	a = a - 1;
}

print "a should be 0: " + a + " and b should be 2: " + b;